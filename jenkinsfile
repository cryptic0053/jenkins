pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Show hello.txt content') {
            steps {
                script {
                    def content = readFile 'hello.txt'
                    echo "hello.txt content:\n${content}"
                }
            }
        }
    }
}
